import { HowerdisplayDirective } from './howerdisplay.directive';

describe('HowerdisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new HowerdisplayDirective();
    expect(directive).toBeTruthy();
  });
});
